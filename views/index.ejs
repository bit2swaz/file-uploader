<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <nav class="navbar">
            <div class="nav-brand"><%= title %></div>
            <div class="nav-user">
                Welcome, <%= user.name || user.email %>
                <a href="/auth/logout" class="btn btn-logout">Logout</a>
            </div>
        </nav>

        <div class="content">
            <h1>Welcome to <%= title %></h1>
            <p>You are now logged in and can start uploading files!</p>
        </div>
    </div>
</body>
</html> 